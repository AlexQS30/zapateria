package com.back.zapateria.repository;

import com.back.zapateria.model.User;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class UserRepositoryTest {

    // Pruebas comentadas — revisa antes de descomentar.
    // @Test
    // void save_and_findByEmail() {
    //     UserRepository repo = // obtener repo (mock o in-memory)
    //     User u = new User(...);
    //     repo.save(u);
    //     Optional<User> found = repo.findByEmail(u.getEmail());
    //     assertTrue(found.isPresent());
    // }
}
